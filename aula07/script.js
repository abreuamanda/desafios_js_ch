// número primo: crie uma funcionalidade que solicita para que o usuário informe um número até que o número informado seja primo

const num = parseInt(prompt('Informe um número:'));

while (true) {
    num = parseInt(prompt('Informe um número:'));

    if 
}